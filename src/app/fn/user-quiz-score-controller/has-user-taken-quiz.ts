/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface HasUserTakenQuiz$Params {
  userId: number;
  quizId: number;
}

export function hasUserTakenQuiz(http: HttpClient, rootUrl: string, params: HasUserTakenQuiz$Params, context?: HttpContext): Observable<StrictHttpResponse<boolean>> {
  const rb = new RequestBuilder(rootUrl, hasUserTakenQuiz.PATH, 'get');
  if (params) {
    rb.path('userId', params.userId, {});
    rb.path('quizId', params.quizId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: String((r as HttpResponse<any>).body) === 'true' }) as StrictHttpResponse<boolean>;
    })
  );
}

hasUserTakenQuiz.PATH = '/score/user/{userId}/quiz/{quizId}/exists';
