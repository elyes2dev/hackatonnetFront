/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserQuizScore } from '../../models/user-quiz-score';

export interface GetQuizScores$Params {
  quizId: number;
}

export function getQuizScores(http: HttpClient, rootUrl: string, params: GetQuizScores$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<UserQuizScore>>> {
  const rb = new RequestBuilder(rootUrl, getQuizScores.PATH, 'get');
  if (params) {
    rb.path('quizId', params.quizId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<UserQuizScore>>;
    })
  );
}

getQuizScores.PATH = '/score/quiz/{quizId}';
