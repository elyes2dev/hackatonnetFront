/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Quiz } from '../../models/quiz';

export interface UpdateQuiz$Params {
  id: number;
      body: Quiz
}

export function updateQuiz(http: HttpClient, rootUrl: string, params: UpdateQuiz$Params, context?: HttpContext): Observable<StrictHttpResponse<Quiz>> {
  const rb = new RequestBuilder(rootUrl, updateQuiz.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Quiz>;
    })
  );
}

updateQuiz.PATH = '/quiz/{id}';
