import { FilterByNameAndThemePipe } from './filter-by-name-and-theme.pipe';

describe('FilterByNameAndThemePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNameAndThemePipe();
    expect(pipe).toBeTruthy();
  });
});
